({
  // where the built files go
  dir: "${project.build.directory}/classes/META-INF/resources/rbs",
  // where the optimizer looks for css/js files
  appDir: "${basedir}/src/main/resources/META-INF/resources/rbs",
  // the assumed base URL for the require
  baseUrl: "",
  modules: []
})